import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { STLLoader } from 'three/addons/loaders/STLLoader.js';

// TEMP: full square-cut app.js push pending — reload after next commit
console.error('Nest Optimizer: incomplete app.js push. Waiting for full square-cut build.');
document.body.innerHTML = '<p style="color:#f87171;padding:40px;font-family:sans-serif">Updating square-cut engine… hard-refresh in a moment.</p>';
